# 🎲 Data Science & ML Bootcamp - 설계 철학 및 출제 원리 (Design Philosophy)

본 프로젝트는 단순한 파이썬 문법 암기 테스트를 넘어서, **실제 데이터 분석 및 머신러닝 현업에서의 문제 해결 능력**을 훈련하기 위한 목적으로 설계되었습니다. 

추후 문제 은행을 확장하거나 시스템을 수정할 때, 본 문서에 명시된 세부적인 아키텍처 규칙과 룰셋(Rule Set)을 반드시 준수하여 시스템의 통일성을 유지해야 합니다.

---

## 🎯 1. 핵심 커리큘럼 생태계 (Target Stack)
이 시스템은 특정 라이브러리에 국한되지 않으며, **데이터 사이언티스트/분석가**가 갖춰야 할 아래의 풀스택 도구들을 훈련시킵니다.
* **Python Base:** 기초 프로그래밍 및 자료구조
* **Numpy / Pandas:** 고성능 수치 연산 및 정형 데이터 랭글링(Data Wrangling)
* **Matplotlib / Seaborn:** 데이터 시각화 및 인사이트 도출
* **Scikit-learn:** 머신러닝 모델링 및 파이프라인 구축

---

## 🎲 2. 시스템 출제 로직 및 룰셋 (Exam & Study Logic)

시스템은 두 가지 완전히 다른 목적의 학습 트랙(Track)을 제공하며, 각 트랙은 엄격한 룰셋에 의해 동작합니다.

### A. 실전 모의고사 (Exam Mode) - "비율 고정 출제"
모의고사는 완전한 랜덤이 아니라 **"실제 수업 커리큘럼(Day1~Day4)과의 연계성"**을 위해 8:2의 엄격한 난이도 배분을 따릅니다.
* **총 문항 수:** 20문항 (고정)
* **기초/정규 수준 (16문항 - 80%):** 데이터 로드, 미리보기, 필터링, 결측치 등 현업에서 가장 많이 쓰이는 기초 체력 위주의 문제.
* **심화/고급 수준 (4문항 - 20%):** 데이터 병합(Join), 피벗 테이블 생성, 시계열 파싱, 머신러닝 데이터 분할 및 객체 생성 등 변별력을 가르는 고급 문제.

### B. 학습 모드 (Study Mode) - "Endless 무한 루프"
스터디 모드는 모의고사와 달리 문항 수 제한이나 섞기(Shuffle)의 개념이 존재하지 않습니다.
* **무한 문제은행:** 수백, 수천 번을 클릭해도 끊임없이 새로운 문제가 단건으로 추출(Single Quiz Generator)됩니다.
* **엄격한 정답률 추적:** 3번의 재시도 기회가 주어지지만, 상단 대시보드의 **"첫 시도 정답률(Accuracy)"**은 오직 1트(First-try)에 맞힌 경우에만 정답으로 산정하여 학습자에게 냉정한 현재 실력을 인지시킵니다.

### C. 객관식 / 주관식 믹스 룰 (Mixed Type Rule)
모바일 환경의 타이핑 피로도를 줄이되, PC 환경에서의 코딩 감각을 잃지 않도록 두 가지 방식을 섞습니다.
* 전체 문제 중 **단 10% (20문제 중 2문제 수준)**는 강제로 **주관식(직접 타이핑)**으로 전환됩니다.
* 나머지는 큼지막한 보기 타일을 클릭하는 객관식(Radio)으로 제공되어 빠른 속도감을 유지합니다.

---

## 🛠 3. 문제 변형 철학: 상황 기반 난수화 (Scenario-based Randomization)

문제 출제 시 단순한 숫자 변경을 금지하며, 현업의 **맥락(Context)**을 담은 Dictionary Array에서 무작위로 시나리오를 추출해야 합니다.

1. **파라미터 튜닝 및 행간(Nuance)의 이해 시나리오:**
   * 단순히 함수의 존재 유무를 묻는 것을 넘어, **파라미터를 다르게 썼을 때 결과물이 어떻게 달라지는지(행간의 의미)**를 묻습니다.
   * `axis=0`(행 방향) vs `axis=1`(열 방향)의 구조적 차이 묻기.
   * `keep='first'` vs `keep='last'`를 사용하여 중복 데이터 처리 시 어떤 데이터가 살아남는지 묻기.
   * "교집합만 남겨라" ➡️ `how='inner'`, "왼쪽 테이블 데이터는 모두 살려라" ➡️ `how='left'`
2. **분석 파이프라인 시퀀스 난수화:**
   * "머신러닝 전처리" ➡️ `결측치 처리 ➡️ 범주형 인코딩 ➡️ 스케일링`
   * "시계열 분석" ➡️ `Datetime 변환 ➡️ 시간순 정렬 ➡️ 이동평균 계산`
3. **실무 장애(Troubleshooting) 시나리오:**
   * 대용량 파일 MemoryError ➡️ `chunksize` 파라미터 활용
   * 공공데이터 인코딩 에러 ➡️ `encoding='cp949'`

### 💀 매력적인 오답 (Distractors) 생성 가이드라인
객관식 보기 생성 시, 반드시 실무자들이 코딩할 때 **가장 흔히 저지르는 문법적 실수**를 유도해야 합니다.
* 파라미터 이름 속이기: `kind` 대신 `type` (`df.plot(type='bar')`)
* 패키지 혼동: `pd.barplot()` vs `df.plot.bar()`
* 인자 축/순서 반전: `split(y, X)` 등 혼란을 유도하는 교묘한 오답 생성.

---

## 🎨 4. UI/UX 원칙 (Glassmorphism & UX)
* **글래스모피즘(Glassmorphism) 강제 적용:** 모든 컨테이너와 모달 박스에는 반투명 백그라운드(`rgba`)와 백드롭 블러(`backdrop-filter: blur(20px)`)를 적용하여 깊이감과 프리미엄 질감을 냅니다.
* **Streamlit 네이티브 UI 은닉:** `.streamlit/config.toml`을 통해 상단 툴바와 하단 워터마크를 제거하여 단독 웹 애플리케이션의 형태를 유지합니다.
* **타일형 액션 버튼:** 기본 라디오 체크박스 디자인을 숨기고, 큼직하게 렌더링 된 카드 형태의 Tile 버튼을 통해 모바일 터치 접근성을 극대화합니다.
