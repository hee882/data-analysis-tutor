# 🎲 Data Science & ML Bootcamp - 설계 철학 및 출제 원리 (Design Philosophy)

본 프로젝트는 단순한 파이썬 문법 암기 테스트를 넘어서, **실제 데이터 분석 및 머신러닝 현업에서의 문제 해결 능력**을 훈련하기 위한 목적으로 설계되었습니다. 

> **🎯 핵심 커리큘럼 생태계 (Target Stack)**
> * **Python Base:** 기초 프로그래밍 및 자료구조
> * **Numpy / Pandas:** 고성능 수치 연산 및 정형 데이터 전처리
> * **Matplotlib / Seaborn:** 데이터 시각화 및 인사이트 도출
> * **Scikit-learn:** 머신러닝 모델링 및 파이프라인 구축

추후 문제 은행을 확장하거나 시스템을 수정할 때, 이 풀스택 생태계 전반을 아우르는 **"상황 기반 난수화(Scenario-based Randomization)"** 철학을 유지해야 합니다.

---

## 1. 숫자 변형을 넘은 "상황(Scenario)의 난수화"
난수(Random)의 개념을 단순히 숫자를 1에서 5로 바꾸는 것에 국한하지 않습니다. 현업에서 마주치는 **다양한 요구사항, 에러 메시지, 절차적 맥락을 Dictionary Array(풀)로 구축**하고, 여기서 하나를 무작위 추출(
andom.choice)하여 문제를 동적으로 생성합니다.

### A. 함수 파라미터 활용법 난수화
단순히 pd.merge()의 존재 여부를 묻지 않고, 매번 바뀌는 **비즈니스 요구사항**을 던집니다.
* **예시 시나리오 풀:**
  * "교집합만 남겨라" ➡️ how='inner'
  * "왼쪽 테이블 데이터는 모두 살려라" ➡️ how='left'
  * "양쪽 기준 컬럼명이 다르다" ➡️ left_on='A', right_on='B'

### B. 분석 파이프라인(절차) 난수화
분석 타겟 데이터의 성격이 변하면, 전처리 파이프라인의 **올바른 순서**도 동적으로 변하게 설계합니다.
* **예시 시나리오 풀:**
  * "머신러닝 전처리" ➡️ 결측치 처리 ➡️ 범주형 인코딩 ➡️ 스케일링
  * "시계열 분석" ➡️ Datetime 변환 ➡️ 시간순 정렬 ➡️ 이동평균 계산
* **오답 생성 철학:** 정답의 순서를 교묘하게 뒤바꾸어 혼란을 유도하는 오답 보기(Distractors)를 생성합니다.

### C. 실무 장애(Troubleshooting) 난수화
데이터를 다룰 때 흔히 겪는 **치명적인 에러 상황**을 발생시키고, 그 해결책을 묻습니다.
* **예시 시나리오 풀:**
  * "대용량 CSV 파일 MemoryError" ➡️ chunksize 파라미터 활용
  * "한국어 공공데이터 UnicodeDecodeError" ➡️ encoding='cp949' 추가
  * "날짜와 문자열 혼재로 인한 변환 에러" ➡️ errors='coerce' 강제 치환 적용

---

## 2. 매력적인 오답(Distractors) 생성 철학
객관식의 변별력은 오답 보기의 퀄리티에서 나옵니다. 무의미한 코드를 넣지 않고, **실무자들이 코딩할 때 가장 흔히 헷갈리는 문법적 실수**를 분석하여 오답을 조합합니다.
* kind 대신 	ype 파라미터 사용 유도 (df.plot(type='bar'))
* pandas 최상위 함수와 DataFrame 메서드의 혼동 유도 (pd.barplot() vs df.plot.bar())
* 잘못된 체이닝(Chaining) 방식 유도

## 3. UI/UX: 철저한 사용자 중심 경험 (Glassmorphism & Mobile-first)
* **모바일 최적화:** 타이핑의 피로도를 없애기 위해 객관식(라디오 타일) 위주로 구성.
* **타일형 카드 디자인:** 체크박스가 아닌 큼지막한 타일 형태의 글래스모피즘(Glassmorphism) UI를 적용해 터치 편의성과 심미성을 극대화.
* **루틴 혼합:** 20문제 중 무작위 2문제를 주관식으로 강제 전환하여, 적절한 긴장감을 부여하는 러시안 룰렛 로직 채택.

